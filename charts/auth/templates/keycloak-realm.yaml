apiVersion: keycloak.org/v1alpha1
kind: KeycloakRealm
metadata:
  name: example-keycloakrealm
spec:
  realm:
    id: basic
    realm: basic
    enabled: true
    displayName: Basic Realm
  instanceSelector:
    matchLabels:
      app: sso